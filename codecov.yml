coverage:
  status:
    projects:
      dotnet:
        flags: dotnet
      npm:
        flags: npm          
flags:
  dotnet:
    paths:
      - *
  npm:
    paths:
      - apps/CardHero.NetCoreApp.TypeScript
