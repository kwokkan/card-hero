coverage:
  status:
    projects:
      dotnet:
        flags: dotnet
      npm:
        flags: npm          
flags:
  dotnet:
    fixes:
      - "root/project/::"
    paths:
      - apps
      - shared
      - src
  npm:
    paths:
      - apps/CardHero.NetCoreApp.TypeScript
